# MLflow Infrastructure Environment Variables
# Copy this file to .env if you need to customize credentials
# NEVER commit .env to version control!

# PostgreSQL Database
POSTGRES_USER=mlflow
POSTGRES_PASSWORD=mlflow123
POSTGRES_DB=mlflow_db

# MinIO S3 Storage
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_BUCKET=mlflow-bucket

# MLflow S3/MinIO credentials (should match MinIO credentials)
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin123

# Notes:
# - These match defaults in infrastructure/mlflow/secrets.yaml.template
# - For production, use stronger passwords!
# - For local development with port-forwarding:
#   - MLflow UI: http://localhost:5000
#   - MinIO Console: http://localhost:9001
#   - MinIO S3 API: http://localhost:9000
